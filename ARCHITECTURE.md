# الوثيقة المعمارية — صفحة رسوم نشر الإعلان «سوق الرياض»

> الإصدار: 1.0 — الحالة: بانتظار الاعتماد
> الهدف: قياس استعداد أعضاء مجموعة سوق الرياض لدفع رسوم نشر الإعلان، عبر صفحة هبوط عربية آمنة، بسيطة، متجاوبة، جاهزة للنشر على GitHub Pages.

---

## 1. ملخص القرارات المعمارية

| القرار | الخيار | السبب |
|--------|--------|-------|
| الاستضافة | GitHub Pages (ثابتة 100%) | المطلوب صراحةً، صفر تكلفة، بدون خادم |
| التقنية | HTML + CSS + JS خام، بدون بَيلد | بساطة قصوى، سهولة الفحص والتعديل والتطوير |
| التوجيه | Hash-Based Routing (`#/`, `#/confirm`, `#/admin`) | يعمل على المسارات الفرعية لـ GitHub Pages دون إعدادات خادم |
| الإعدادات | ملف واحد `config.js` | جميع الباقات والرسائل والسياسات قابلة للتعديل من مكان واحد |
| تخزين البيانات | `localStorage` (نموذج JSON منظم) | بدون خادم، بدون قواعد بيانات، صفر بيانات حساسة |
| الدفع | PayPal JS SDK بمفاتيح **Placeholder** + وضع تجريبي محلي | المطلوب: تكامل آمن بدون تخزين مفاتيح أو بيانات حساسة |
| لوحة الإدارة | صفحة خفيفة برمز دخول بسيط + تصدير CSV/JSON | إدارة يدوية بلا نظام معقّد |
| اللغة/التصميم | عربية RTL، خطوط عربية (Tajawal/Cairo) | تجربة سلسة ومألوفة للأعضاء |

---

## 2. التدفق الهيكلي (Structural Flow)

```mermaid
flowchart LR
  A[النشر في الجروب كالمعتاد] --> B[وصف الجروب + المنشور المثبّت يوضّحان إجراء رسوم النشر]
  B --> C[فتح صفحة الدفع الرسمية]
  C --> D[قسم الثقة: الفائدة + الخصوصية + سياسة القبول/الرفض + مدة المراجعة]
  D --> E[النموذج القصير]
  E --> F[اختيار الباقة: فردي 40 / تجاري 80]
  F --> G{تكوين الدفع}
  G -- MOCK --> H[محاكاة دفع داخل الصفحة]
  G -- PAYPAL --> I[نافذة PayPal (Sandbox)]
  H --> J[إتمام عملية الدفع]
  I --> J
  J -- نعم --> K[توليد رقم مرجعي فريد RS-XXXXXXXX]
  K --> L[حفظ الطلب في localStorage]
  L --> M[صفحة تأكيد احترافية بالرقم المرجعي]
  J -- لا / إلغاء --> N[رسالة إلغاء لطيفة + عودة إلى النموذج]
  M --> O[الإدارة تفتح اللوحة وترى الطلبات]
  O --> P[مطابقة اسم فيسبوك / عنوان الإعلان مع الدفع في حساب PayPal]
  P --> Q[موافقة أو رفض يدوي داخل اللوحة]
  Q --> R[الإدارة تنشر الإعلان داخل فيسبوك بعد القبول]
```

### 2.1 قيود تصميمية صريحة
- **الدفع لا يضمن النشر**: القبول النهائي خاضع لسياسة الجروب، شريطة ألا يخالف الإعلان سياسة النشر على فيسبوك؛ ويُذكر ذلك صراحةً في الصفحة.
- **النظام ليس قناة مراسلة**: لا رسائل فردية، لا واتساب داخلي، لا رابط مراسلة بعد الطلب. بيانات الاتصال (إيميل/واتساب) لجمع المعلومات فقط إن وُجدت.
- **رقم واتساب اختياري** ولا يُستخدم إلا مستقبلاً إن اقتضى تطوير المنتج (قرار موثّق، غير مفعّل حاليًا).
- **الإحالة على ما بعد الدفع**: الصفحة ترشد المستخدم للانتظار مدة المراجعة المذكورة في الإعدادات قبل المتابعة داخل الجروب.

---

## 3. هيكل المجلدات (Folder Structure)

```
souq-riyadh-landing/
├── README.md                     # دليل الاستخدام والنشر (للمشرف)
├── ARCHITECTURE.md               # هذه الوثيقة
├── index.html                    # نقطة الدخول الوحيدة (SPA)
├── CONTENT.md                    # (اختياري) نصوص جاهزة لوصف الجروب والمنشور المثبّت
├── assets/
│   ├── css/
│   │   └── styles.css            # التنسيق الكامل RTL + استجابة الجوال
│   ├── js/
│   │   ├── config.js             # ⭐ ملف الإعدادات الوحيد (باقات/رسائل/سياسات/مفاتيح)
│   │   ├── app.js                # نقطة التشغيل + التهيئة
│   │   ├── router.js             # توجيه النوافذ #/
│   │   ├── storage.js            # طبقة التخزين (localStorage) — قابلة للاستبدال مستقبلاً
│   │   ├── orders.js             # منطق الطلبات (إنشاء/تحديث/حالة/بحث/تصدير)
│   │   ├── reference.js          # توليد الرقم المرجعي الفريد
│   │   ├── paypal.js             # غلاف PayPal (MOCK / Sandbox) بالمفاتيح المكانية
│   │   └── views/
│   │       ├── landing.js        # صفحة الهبوط + النموذج
│   │       ├── confirmation.js   # صفحة التأكيد
│   │       └── admin.js          # لوحة الإدارة
│   ├── icons/                    # أيقونات SVG محلية
│   └── opengraph/
│       └── og-default.png        # صورة المشاركة (اختياري)
```

**سبب فصل الملفات:** كل ملف مسؤولية واحدة؛ ليُمكّن التطوير المستقبلي (استبدال طبقة التخزين، إضافة باك إند، إضافة باقات) دون إعادة كتابة كل شيء.

---

## 4. ملف الإعدادات الوحيد `config.js`

بنية مفصّلة (كل قيمة قابلة للتعديل من مكان واحد):

```js
window.RIYADH_CONFIG = {
  // الباقات
  packages: [
    { id: "single",   name: "إعلان فردي",   priceSar: 40, pricePaypal: "40.00", note: "" },
    { id: "business", name: "إعلان تجاري",  priceSar: 80, pricePaypal: "80.00", note: "" }
  ],
  currency: "SAR",

  // السياسات والثقة
  policy: {
    reviewHours: 12,                     // مدة المراجعة المعروضة
    acceptanceTermsHtml: "...",          // نص سياسة القبول/الرفض
    privacyHtml: "...",                  // نص الخصوصية
    paymentNotGuarantee: true,           // رسالة: الدفع لا يضمن النشر
    adminEmail: "admin@souq.example"     // للتوثيق فقط (لا يُستدعى)
  },

  // التواصل / الجروب
  group: {
    groupUrl: "",                        // رابط الجروب (إشارة فقط، لا قناة مراسلة)
    brand: "سوق الرياض"
  },

  // الدفع
  paypal: {
    mode: "MOCK",                        // "MOCK" للعرض | "SANDBOX" | "LIVE" لاحقاً
    clientId: "YOUR_PAYPAL_SANDBOX_CLIENT_ID",  // مفتاح Placeholder — لا يُخزَّن سري
    currency: "USD",                     // PayPal لا يدعم SAR حاليًا؛ تُحسب المعادلة في config
    message: "المبلغ يُحول إلى دولار أمريكي عند الدفع"
  },

  // لوحة الإدارة
  admin: {
    passcode: "0000",                    // رمز بسيط — يُغيَّر قبل الإنتاج
    storageKey: "souq_riyadh_orders_v1"
  }
};
```

> **أمن المفاتيح:** Client ID عام بطبيعته؛ الـ Secret لا يُوضع إطلاقًا في أي ملف أمامي. لا يُخزَّن أي مفتاح أو بيانات دفع في `localStorage`. الملاحظة موثّقة داخل `paypal.js` والـ README.

---

## 5. نموذج البيانات (لا بيانات حساسة)

كائن الطلب المحفوظ:

```json
{
  "id": "RS-8F2KQ-7M4XA",
  "createdAt": "2026-09-09T10:15:30.000Z",
  "packageId": "single",
  "packageName": "إعلان فردي",
  "amountSar": 40,
  "form": {
    "fbName": "…",
    "adTitle": "…",
    "link": "… (اختياري)",
    "email": "…",
    "whatsapp": "… (اختياري)"
  },
  "paypal": {
    "status": "PENDING",        // PENDING | COMPLETED | FAILED
    "orderId": "…",             // معرّف بايبال إن وُجد (معلومات فقط)
    "reference": "RS-…"
  },
  "adminStatus": "submitted"    // submitted | approved | rejected
}
```

- لا تُخزَّن كلمات مرور، مفاتيح، أو بيانات بطاقات.
- الحقل `paypal.orderId` يُستخدم فقط للمطابقة اليدوية داخل حساب PayPal، لا للمعالجة.

---

## 6. تصميم الواجهة (UI Design)

### 6.1 النمط والهوية
- **RTL** كامل، `lang="ar"`, `dir="rtl"`.
- خط **Tajawal** (عناوين ووزن ثقيل) + احتياطي نظامي.
- نظام ألوان ثقة: خلفية فاتحة نظيفة، لون أساسي أخضر زيتوني داكن («سوق»)، لون تأكيدي/زر أساسي لون ملكي غامق، ألوان حالة: أخضر=موافق، أحمر=مرفوض، برتقالي=قيد المراجعة.
- تباين قوي للأزرار والروابط، حالة تركيز واضحة (Accessibility).

### 6.2 الشاشات (Views)
1. **الهبوط (`#/`)**: رأس بصري بالعلامة ↵ نبذة فائدة بثلاث بطاقات (وصول أوسع/نظام رسمي/مراجعة سريعة) ↵ قسم الثقة (الخصوصية + سياسة القبول والرفض + مدة المراجعة) ↵ **النموذج القصير** (اسم فيسبوك، رابط اختياري، عنوان الإعلان، الإيميل، واتساب اختياري) ↵ **اختيار بطاقة** بشكل بديل (Radio مدمج بالسعر) ↵ زر دفع عبر PayPal ↵ تنبيهات الجانب "الدفع لا يضمن النشر".
2. **التأكيد (`#/confirm?ref=…`)**: علامة نجاح، الرقم المرجعي بارز بقابلية النسخ، ملخص الطلب، إرشاد لانتظار مدة المراجعة، لمحة «ماذا يحدث بعدها».
3. **اللوحة (`#/admin`)**: بوابة رمز دخول بسيط ↵ جدول الطلبات (مرجع/تاريخ/الباقة/اسم فيسبوك/عنوان الإعلان/الحالة) ↵ بحث بالاسم أو العنوان (للمطابقة مع الدفع) ↵ فلترة حسب الحالة ↵ زر موافقة/رفض لكل صف ↵ زر تصدير CSV/JSON.
4. **404 داخلي** للنوافذ غير المعروفة.

### 6.3 التجاوب
- Mobile-first: نموذج بعمود واحد، جدول اللوحة يتحوّل إلى بطاقات على الجوال، أزرار كاملة العرض، أهداف لمس ≥ 44px.

---

## 7. تكامل PayPal — الوضع الحالي

| الوضع | الاستخدام | التفاصيل |
|-------|-----------|----------|
| `MOCK` | العرض والحفظ والتجربة | محاكاة نافذة دفع محلية تُنهي الطلب فورًا لتوليد رقم مرجعي؛ آمنة للتطوير |
| `SANDBOX` | اختبار حقيقي قبل الإطلاق | تحميل PayPal JS SDK بمفتاح `clientId` Placeholder، زر Smart Buttons |
| `LIVE` | إنتاج فعلي | مستقبلي — يتطلب النقطة التالية |

> **قيد صريح:** على GitHub Pages الثابتة لا يمكن التقاط الدفع (Capture) بعد موافقة المستخدم من جهة الخادم، لأن ذلك يتطلب Secret. لذلك:
> - الوضع الحالي: إنشاء الطلب + رقم مرجعي + حالة `PENDING` والمطابقة اليدوية داخل اللوحة.
> - الانتقال لإنتاج حقيقي: إضافة طبقة دالة خادم (Cloudflare Pages / Netlify Functions أو Backend بسيط) دون تكسير الواجهة — نقطة التطوير الموثقة في القسم 9.

---

## 8. خصوصية الأمان والعقود القانونية (في الصفحة)
- إفصاح واضح: أمن الدفع عبر PayPal، لا نخزّن بيانات بطاقة.
- نص القبول/الرفض: «الدفع يغطي رسوم المراجعة والإدراج ضمن شروط الجروب، ولا يُعد ضمانًا للنشر».
- مدة المراجعة مذكورة رقمياً من `config.policy.reviewHours`.
- رفض صريح لاستخدام الصفحة كقناة مراسلة.

---

## 9. قابلية التطوير (Extension Points)
1. **طبقة تخزين قابلة للاستبدال**: `storage.js` مجرّد عبر واجهة `get/save/update` → لاحقًا استبدال بـ Backend/Cloud DB دون لمس الواجهة.
2. **دوال خادم للدفع**: إضافة `functions/capturePayPal.js` وتفعيل `LIVE`.
3. **بوابات إضافية**: إضافة بوابة Apple Pay / STC Pay مستقبلاً داخل `payment-providers/`.
4. **الباقات**: إضافة عناصر فقط داخل `config.js`.
5. **إحصائيات**: قراءة الطلبات وتقديم تقرير استعداد الدفع.
6. **إرسال إشعارات** (اختياري مستقبلاً) عبر خدمة Forms وليس قناة مراسلة داخلية.

---

## 10. خطة التنفيذ (Implementation Plan)

| # | الخطوة | المخرجات | الحالة |
|---|--------|----------|--------|
| 1 | اعتماد هذه الوثيقة | — | ⏳ بانتظارك |
| 2 | الهيكل الأساسي | `index.html` + `assets/` + `config.js` | — |
| 3 | التنسيق والثيم | `styles.css` (RTL, متجاوب) | — |
| 4 | الخدمات | `storage.js`, `reference.js`, `orders.js` | — |
| 5 | الدفع | `paypal.js` (MOCK + Sandbox Placeholder) | — |
| 6 | التوجيه | `router.js` + `app.js` | — |
| 7 | الواجهات | `landing.js`, `confirmation.js`, `admin.js` | — |
| 8 | المحتوى | `CONTENT.md`: نص وصف الجروب + المنشور المثبّت + رسائل الإعداد | — |
| 9 | التحقق | اختبار محلي (npx serve) جميع المسارات + التجربة الجوالية | — |
| 10 | التوثيق | `README.md` خطوات النشر على GitHub Pages + تغيير الرمز | — |

---

## 11. نطاق غير مشمول (Out of Scope حالياً)
- لا Backend، لا قواعد بيانات، لا حسابات مستخدمين.
- لا معالجة دفع حقيقية (فقط تكامل بمفاتيح مكانية).
- لا إشعارات داخلية ولا قناة مراسلة.
- لا صرف/استرداد آلي (يُدار داخل الجروب).

---

> **القرار التالي المطلوب من العميل:** اعتماد الوثيقة للبدء بالتنفيذ، مع تأكيد: وضع الدفع الافتراضي (`MOCK`)، ورمز اللوحة الافتراضي `0000` للتعديل لاحقًا.